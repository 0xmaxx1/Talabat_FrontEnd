@model IReadOnlyList<ProductBrand>
@{
    ViewData["Title"] = "Index";
}

<h1>All Product Brands</h1>
<hr />
<br />

<a asp-action="Create" class="btn btn-outline-primary">Add New Product Brand</a>

@if (Model.Count > 0)
{
    <table class="mt-3 table table-bordered table-striped table-hover dt-responsive nowrap">
        <thead class="bg-dark text-white text-center">
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>Details</th>
                <th>Update</th>
                <th>Delete</th>
            </tr>
        </thead>

        <tbody>
            @foreach (var brand in Model)
            {
                <tr class="text-center align-middle">
                    <td>@brand.Id</td>
                    <td>@brand.Name</td>

                    <partial name="_ButtonsPartialView" model="@brand.Id.ToString()" />
                </tr>
            }
        </tbody>

    </table>
}
else
{
    <div class="mt-3 p-3 alert-warning">
        <h2>There's No Brands'</h2>
    </div>
}

